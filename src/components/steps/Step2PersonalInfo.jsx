/**
 * Step 2 — Personal Information (Spec B2.1).
 * Placeholder pending Day 3: name, DOB (age 21–65), gender, marital status,
 * parents' names, email, mobile (+ alternate).
 */
function Step2PersonalInfo() {
  return <p className="text-slate-500">Applicant personal details will be captured here.</p>;
}

export default Step2PersonalInfo;
