/**
 * Step 4 — Address Information (Spec B2.1, A3.3).
 * Placeholder pending Day 5: current address with PIN-code auto-fill,
 * residence type, years at address, permanent address (conditional).
 */
function Step4Address() {
  return <p className="text-slate-500">Address details with PIN-code lookup will be captured here.</p>;
}

export default Step4Address;
