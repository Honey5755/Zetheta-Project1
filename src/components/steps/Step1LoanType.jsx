/**
 * Step 1 — Loan Type Selection & Basic Information (Spec B2.1).
 * Placeholder pending Day 3: loan type radio, amount (per-type range),
 * tenure, purpose, optional referral code.
 */
function Step1LoanType() {
  return (
    <p className="text-slate-500">
      Loan type, amount, tenure and purpose will be captured here.
    </p>
  );
}

export default Step1LoanType;
